# Simulator / web: localhost is fine. Physical iPhone: use your Mac's LAN IP, e.g. http://192.168.1.42:5001
# Find IP: System Settings → Network → Wi‑Fi → Details, or run: ipconfig getifaddr en0
# Optional: put device-only overrides in .env.local (gitignored).


#EXPO_PUBLIC_API_URL=http://localhost:5001
EXPO_PUBLIC_API_URL=https://karmalock-api.onrender.com
